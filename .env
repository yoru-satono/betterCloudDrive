# PostgreSQL
POSTGRES_DB=better_cloud_drive
POSTGRES_USER=bcd_user
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# Storage (SeaweedFS S3)
S3_ACCESS_KEY=admin
S3_SECRET_KEY=change_me_in_production

# JWT (generate with: openssl rand -base64 32)
JWT_ACCESS_SECRET=replace_with_base64_encoded_256bit_secret
JWT_REFRESH_SECRET=replace_with_different_base64_encoded_256bit_secret

# App
BACKEND_PORT=8080
FRONTEND_PORT=3000

# Mailpit (email simulation)
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025
